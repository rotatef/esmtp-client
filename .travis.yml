language: lisp
sudo: required

branches:
  only:
    - master

env:
  matrix:
    - LISP=sbcl

install:
  - curl -L https://github.com/luismbo/cl-travis/raw/master/install.sh | sh

script:
  - cl -e "(ql:quickload :cl-esmtp-client) (cl-esmtp-client-tests:run)"
